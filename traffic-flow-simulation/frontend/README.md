# Frontend

Static browser-based visualizer for the traffic simulation.

## Run locally

```bash
python -m http.server 8080
```

Then open `http://127.0.0.1:8080`.

## Notes

- The frontend expects the FastAPI backend to be running.
- You can change the API base URL from the UI.
- The same folder can be deployed directly to GitHub Pages.
